const numeroPi = 3.14159; // el numero Pi siempre será el mismo por lo que no tiene sentido cambiar el valor en un futuro;

numeroPi = 24 // esto da error

let numeroGoles = 0; // en cambio si almacenador algo que puede ir cambiando en el tiempo debemos permitir reasignar valores usando let

numeroGoles = 1;